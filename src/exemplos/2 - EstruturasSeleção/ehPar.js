let val = 0;

let res = '';
if(val % 2 == 0){               // utilizamos if para verificar se a condição é verdadeira
    res = 'O Valor é par.';     // se a condição for verdadeira, atribuimos 'é par' à variável res
}
else{
    res = 'O Valor é impar.';   // se não, atribuimos 'impar' à variável res
}

console.log(res);